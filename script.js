document.addEventListener('DOMContentLoaded', () => {
    const text = "Hello,  I am Sadia ! , A Web Developer ";
    const typingTextElement = document.getElementById('typing-text');
    typingTextElement.textContent = text;
});
